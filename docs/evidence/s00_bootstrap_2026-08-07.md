# S00 bootstrap evidence – 2026-08-07

## Scope

Greenfield Flutter scaffold created from scratch. No legacy implementation, database migration, remote link, Supabase write or live deployment was performed.

## Decisions

| Item | Value |
|---|---|
| Dart project/package | `teamzone_app` |
| Android application ID | `com.teamzone.teamzone` |
| iOS bundle ID | `com.teamzone.teamzone` |
| Display name | `TeamZone` |
| Platforms | Android, iOS project files, Flutter web |
| Supabase | Local config only; exposed target schemas `api`, `public_api`; no remote link |

## Toolchain

| Tool | Verified value/result |
|---|---|
| Host | Windows 10 Pro 22H2 |
| Flutter / Dart | 3.44.8 / 3.12.2 |
| Java | Microsoft OpenJDK 17.0.20 |
| Android | SDK 36.0.0, licenses accepted |
| Web | Chrome detected |
| Node / npm | 24.19.0 / 11.17.0 |
| Supabase CLI | 2.111.0 |
| Git | 2.55.0.windows.3 |

`flutter doctor -v` passed all required S00 categories. The Visual Studio warning is accepted because native Windows desktop is not a v1 target.

## Verification

| Check | Result |
|---|---|
| Dart format | 4 files, no changes required |
| Flutter analyze | No issues found |
| Flutter widget tests | 1/1 passed |
| Flutter web build | Passed; Wasm dry run passed |
| Android debug build | Passed; APK size 146,083,446 bytes |
| APK manifest | `com.teamzone.teamzone`, target SDK 36, label TeamZone |
| Generated-ID scan | No `com.teamzone.teamzone_app`/`teamzoneApp` remains |
| Secret scan | No secret/service-role/private-key pattern found outside the approved spec snapshot |
| Specification snapshot | 27 Markdown files; aggregate SHA-256 `722c206d2d25e98a2d14f2c73a44f83da4e963af3aa99ce2e03402e1a8df8d4e` |

## Known deviations and deferred checks

- No Android device was connected during S00 (`adb devices -l` returned an empty list), so device runtime smoke is deferred. Android compilation is green.
- iOS cannot be built or signed on this Windows host; only generated project files and bundle identifiers were inspected.
- Four transitive packages report newer versions outside the Flutter SDK's current compatible constraints. `pubspec.lock` is generated and ready for the first commit; no forced upgrade was made.
- Docker/Podman/local PostgreSQL remains intentionally absent. `supabase start` was not run.

## Security and provenance

- `.env`, signing files, Supabase temp/branch state and testcredentials are ignored.
- `.env.example` contains placeholders and explicitly permits only a publishable client key.
- `supabase/config.toml` contains no secret and is not linked to Teamzone6 or the auditproject.
- The approved rebuild specification was copied from `C:\Dev\TeamZone\docs\rebuild_spec`; legacy application code and migrations were not copied.
- The target directory is an independent Git repository on initial branch `main`, with no remote and no commit.

## Rollback

Before S00, `C:\Dev\TeamzoneApp` existed as an empty directory. Until user work or S01 begins, rollback is deletion of only the newly generated greenfield files after explicit approval. Once the project contains user work, rollback must be Git-based and the directory must not be recursively deleted.

## Exit assessment

S00 is technically complete with one non-blocking deferred device-smoke check. S01 remains disabled until separately authorized.
